import { createSlice } from "@reduxjs/toolkit";

export const aSlice = createSlice({
  name: "a",
  initialState: { value: 0 },
  reducers: {},
});
export const {} = aSlice.actions;
export default aSlice.reducer;
